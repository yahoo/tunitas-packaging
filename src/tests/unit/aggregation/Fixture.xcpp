// This is -*- c++ -*- nearly C++2a with Modules TS but in the S.C.O.L.D. stylings that are so popular these days.
// Copyright Yahoo Inc.
// Licensed under the terms of the Apache-2.0 license.
// For terms, see the LICENSE file at https://github.com/yahoo/tunitas-keyston/blob/master/LICENSE
// For terms, see the LICENSE file at https://git.tunitas.technology/all/components/keyston/tree/LICENSE
#divert <fpp>
namespace tests::unit::aggregation { class Fixture; }
#endiv
#divert <hpp>
struct tests::unit::aggregation::Fixture : public rigging::suite::Fixture {
  static auto suite() -> Suite;
  //
  auto test_usage_Count() -> void;
  auto test_usage_Simple() -> void;
  auto test_usage_Summation() -> void;
  auto test_usage_Histogram() -> void;
  auto test_usage_Popularity() -> void;
};
#endiv
#divert <cpp>
#import tests.unit.aggregation.io.Fixture
#import tests.unit.aggregation.Limits
auto tests::unit::aggregation::Fixture::suite() -> Suite {
  auto series = Stream{"aggregation"sv};
  series << []{
             auto use = Stream{"usage"sv};
             use << add("Count", &Fixture::test_usage_Count)
                 << add("Simple", &Fixture::test_usage_Simple)
                 << add("Summation", &Fixture::test_usage_Summation)
                 << add("Histogram", &Fixture::test_usage_Histogram)
                 << add("Popularity", &Fixture::test_usage_Popularity)
                 << END;
             return use;
           }()
         << add<io::Fixture>()
         << add<Limits>()
         << END;
  return settle(series);
}
#import tunitas.keyston.aggregation.Count
#import tunitas.keyston.aggregation.Simple
#import tunitas.keyston.aggregation.Summation
#import tunitas.keyston.aggregation.Histogram
#import tunitas.keyston.aggregation.Popularity
namespace tests::unit::aggregation { 
  auto Fixture::test_usage_Count() -> void {
    using Subject = library::Count;
    auto subject0 = Subject{0};
    auto subject1 = Subject{1};
    swap(subject0, subject1);
    require(ASSERTION, 0u == subject1.count());
  }
  auto Fixture::test_usage_Simple() -> void {
    using Subject = library::Simple;
    auto subject0 = Subject{0};
    auto subject1 = Subject{1};
    swap(subject0, subject1);
    require(ASSERTION, 0u == underlying(subject1));
    require(ASSERTION, 1u == underlying(subject0));
  }
  auto Fixture::test_usage_Summation() -> void {
    using Subject = library::Summation;
    auto subject0 = Subject{0};
    auto subject1 = Subject{1};
    swap(subject0, subject1);
    require(ASSERTION, 0u == underlying(subject1));
    require(ASSERTION, 1u == underlying(subject0));
  }
  auto Fixture::test_usage_Histogram() -> void {
    using Subject = library::Histogram;
    auto subject = Subject{};
    require(ASSERTION, 0 < subject.size());
  }
  auto Fixture::test_usage_Popularity() -> void {
    using Subject = library::Popularity;
    auto subject = Subject{33uz, library::Count{99}};
    require(ASSERTION, 33uz == subject.size());
  }
}
#endiv
